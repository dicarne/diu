module "my"
from you use {n1}

node Main {             // 声明一个节点
    fn main() {         // 声明一个函数
        let a = {}
        a.hello = 0
        sys:print(a.hello)
    }
    fn add(a: num, b: num) {
        return a + b
    }
}
