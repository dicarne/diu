module "my"
from you use {n1}

node Main {             // 声明一个节点
    fn main() {         // 声明一个函数
        sys:print(1)
        cy(10)
    }
    fn add(a: num) {
        return a + this.b
    }
    fn cy(a: num) {
        a = a-1
        sys:print(a)
        let n = n1:new()
        n:echo()
        if(a>0){
            run cy(a)
        }
    }
}
