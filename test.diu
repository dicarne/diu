module "my"
from debug use {assert}

node Main {             // 声明一个节点
    static fn main() {         // 声明一个函数
        let a = {}
        a["b"]["c"] = 1
        debug:assert(1, a["b"]["c"], 1)       
    }
    // fn add(a: num, b: num) {
    //     return a + b
    // }
    // fn cy(a: num) {
    //     a = a-1
    //     sys:print(a)
    //     let n = n1:new()
    //     n:echo()
    //     if(a>0){
    //         run cy(a)
    //     }
    // }
}
