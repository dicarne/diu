module "my"
from you use {n1}

node Main {             // 声明一个节点
    fn main() {         // 声明一个函数
        this.num = 2
        n1:func(this)
        sys:print(this.num)
        
    }
    fn add(a: num) {
        return a + this.b
    }
}
