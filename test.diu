module my
from debug use {assert}

node Main {             // 声明一个节点
    fn new() {
    }
    fn main() {         // 声明一个函数
        test2()
        sys:exit() 
    }
    //fn test1() {
    //    let a = {}
    //    a["b"]["c"] = 1
    //    assert:equ(1, a["b"]["c"], 1)
    //
    //    a = 2
    //    assert:equ(2, a, 2)
    //
    //    assert:equ(3, this.newv, 100)
//
    //    let c = run add(2, 3)
    //    sys:print(await c)
    //}
    fn test2() {
        let a = 3
        while(a > 0) {
            sys:print(a)
            if (a == 1) {
                break
            }
            a = a - 1
        }
    }
    //fn add(a: num, b: num) {
    //    return a + b
    //}
    // fn cy(a: num) {
    //     a = a-1
    //     sys:print(a)
    //     let n = n1:new()
    //     n:echo()
    //     if(a>0){
    //         run cy(a)
    //     }
    // }
}
