module "my"
from you use {n1}

node Main {             // 声明一个节点
    fn main() {         // 声明一个函数
        this.b = 1
        sys:print(add(2))
        
    }
    fn add(a: num) {
        return a + this.b
    }
}
