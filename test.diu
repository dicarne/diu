module "my"
use you

node Main {             // 声明一个节点
    fn main() {         // 声明一个函数
        let a = run add(1, 2)
        sys:print(await a)
    }
    fn add(a: num, b: num) {
        return a + b
    }
}
