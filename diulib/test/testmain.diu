module my
from debug use {assert}
from __test__ use {test1}

node Main {             // 声明一个节点
    fn new() {
    }
    fn main() {         // 声明一个函数
        let t = test1:new()
        t:runall()
        sys:exit() 
    }
}

